# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8080/api

# OAuth Providers (handled by backend, but frontend may need to know redirect URLs)
NEXT_PUBLIC_OAUTH_GOOGLE_ENABLED=true
NEXT_PUBLIC_OAUTH_GITHUB_ENABLED=true
